<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/startFragment">
    <fragment
        android:id="@+id/startFragment"
        android:name="com.jommaa.albums.view.fragments.MainFragment"
        android:label="@string/app_name"
        app:layout="@layout/main_fragment">
        <action
            android:id="@+id/action_go_to_details_page"
            app:destination="@id/detailsFragment" />
    </fragment>
    <fragment
        android:id="@+id/detailsFragment"
        android:name="com.jommaa.albums.view.fragments.DetailsFragment"
        android:label="Details"
        app:layout="@layout/details_fragment">
    </fragment>
</navigation>